<h4>New Comment</h4>
<%= form_for @comment, url: "/users/#{current_user.id}/articles/#{@article.id}/comments" do |f| %>
  <p><%= f.text_area :content, :value => 'COMMENT HERE' %><br></p>
<%= f.submit "Submit" %>
<% end %>
